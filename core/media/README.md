# Core Media - دليل الاستخدام

## 📚 نظرة عامة

هذه الحزمة تحتوي على أدوات معالجة الوسائط (فيديو، صور، صوت).

---

## 📦 الملفات

### 1. watermark.py
دوال إضافة اللوجو والعلامات المائية

```python
from core.media.watermark import (
    get_logo_overlay_position,
    apply_simple_watermark,
    apply_animated_watermark,
    apply_watermark
)

# مثال - إضافة لوجو متحرك
result = apply_animated_watermark(
    input_path="video.mp4",
    output_path="output.mp4",
    logo_path="logo.png",
    size=150
)

# مثال - إضافة لوجو ثابت
result = apply_watermark(
    input_path="video.mp4",
    output_path="output.mp4",
    logo_path="logo.png",
    position="top_right",
    size=150
)
```

#### المواضع المتاحة:
- `top_left` - أعلى اليسار
- `top_right` - أعلى اليمين
- `top_center` - أعلى الوسط
- `center_left` - وسط اليسار
- `center` - الوسط
- `center_right` - وسط اليمين
- `bottom_left` - أسفل اليسار
- `bottom_center` - أسفل الوسط
- `bottom_right` - أسفل اليمين

#### أنواع الحركة المتاحة:
- `static` - ثابت تماماً
- `corner_rotation` - دوران في الزوايا
- `bounce` - ارتداد دائري
- `slide` - انزلاق يميناً ويساراً
- `fade` - تلاشي
- `zoom` - تكبير

### 2. progress.py
دوال شريط التقدم والاقتباسات

```python
from core.media.progress import *

# سيتم إضافة الدوال هنا لاحقاً
```

---

## 🔄 استيراد شامل

يمكنك استيراد كل شيء دفعة واحدة:

```python
from core.media import *
```

---

## ⚠️ ملاحظات

- جميع دوال اللوجو تستخدم FFmpeg
- تأكد من توفر FFmpeg في النظام
- الإعدادات الافتراضية يتم جلبها من قاعدة البيانات
- في حالة الفشل، يتم إرجاع `input_path` الأصلي

---

## 📖 المزيد

للمزيد من التفاصيل، راجع:
- `/home/user/Bot-iraq/REFACTORING_REPORT.md`
